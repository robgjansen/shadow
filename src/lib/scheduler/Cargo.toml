[package]
name = "scheduler"
version = "0.1.0"
edition = "2021"

[dependencies]
atomic_refcell = "0.1"
crossbeam = "0.8.4"
libc = "0.2"
nix = { version = "0.29.0", features = ["process", "sched"] }

[dev-dependencies]
rand = "0.8.5"
